/* eslint-env jest */

// import transitionBehavior from './__utils__/transitionBehavior'
// import Transition from '../src'

describe('<Transition />', () => {
  it('', () => {
    expect(true).toBe(true)
  })
  // transitionBehavior(Transition)

  // it('should handle transition with different keys', async () => {
  //   const onEnter = jest.fn(done => done())
  //   const onLeave = jest.fn(done => done())

  //   const Wrapper = ({ view }) => (
  //     <Transition onEnter={onEnter} onLeave={onLeave}>
  //       <p key={view}>{view}</p>
  //     </Transition>
  //   )

  //   const wrapper = mount(<Wrapper view="foo" />)

  //   wrapper.setProps({ view: 'bar' })
  //   await wait(expect(onEnter).toHaveBeenCalled)
  //   await wait(expect(onLeave).toHaveBeenCalled)
  // })
})
