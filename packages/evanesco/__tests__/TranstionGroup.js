/* eslint-env jest */

// import transitionBehavior from './__utils__/transitionBehavior'
// import TransitionGroup from '../src'

describe('<TransitionGroup />', () => {
  it('', () => {
    expect(true).toBe(true)
  })

  // transitionBehavior(TransitionGroup)
})
